class Predictor:
    def __init__(self):
        self.data_folder = f"./csv/"
        self.stock_list = ["AAPL", "AMAZN", "GOOGL", "MSFT", "TSLA"]
        self.window_size_list = [3, 7, 14]
        self.
    def predict(self):
        model = load_model()
    def load_model(model_name):
        pass
    def prepare_data(symbol, window_size):
        pass